compileJava.options.compilerArgs.addAll(['-processorpath', project(':plugin').jar.outputs.files.getFiles()[0], '-Xplugin:CharStat'])

if (!hasProperty('mainClass')) {
    ext.mainClass = 'com.example.test.charstat.Main'
}

dependencies {
    compile project(':plugin')
}

